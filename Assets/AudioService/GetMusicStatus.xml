<?xml version="1.0"?>
<doc>
    <assembly>
        <name>GetMusicStatus</name>
    </assembly>
    <members>
        <member name="T:NeteaseMusicHelper">
             <summary>
             网易云音乐进度条的 UI Automation 抓取模块。
            
             设计概览：
             主线程（NeteaseMusicService）只做两件事——通知曲目变化（SetTrack）、
             读取最近一次抓取到的进度快照（TryGetCachedProgress），本身不直接触碰 UIA，
             避免 UIA 调用的不确定耗时拖慢主轮询节奏（尤其是切歌瞬间）。
             真正的 UIA 抓取工作放在独立的后台线程（ProgressWorker）中按固定间隔轮询，
             两者之间通过一份带 generation 版本号的共享状态解耦，防止读到"上一首歌"的进度。
             </summary>
        </member>
        <member name="M:NeteaseMusicHelper.SetTrack(System.String)">
            <summary>
            主线程调用：通知 worker 当前曲目已切换（或播放已结束，track 传 null）。
            这里只更新共享状态并唤醒 worker，不做任何 UIA 操作，因此对主线程而言是零成本的。
            generation 自增用于标记"这是一次新的曲目请求"：worker 据此判断需要等待宽限期、
            丢弃与旧曲目相关的缓存元素与结果。
            </summary>
        </member>
        <member name="M:NeteaseMusicHelper.TryGetCachedProgress(System.Int32@,System.Int32@)">
            <summary>
            主线程调用：非阻塞地读取 worker 最近一次抓取到的进度快照。
            只有当结果的 generation 与当前请求的 generation 一致时才视为有效——
            这避免了"切歌瞬间，worker 还没来得及更新，主线程读到上一首歌进度"的情况。
            </summary>
        </member>
        <member name="M:NeteaseMusicHelper.ProgressWorker">
             <summary>
             后台 worker 主循环。
            
             缓存策略：
             播放器主窗口的查找需要从桌面根节点按进程 PID 枚举子窗口，开销相对较大，
             但窗口标题由操作系统维护、更新及时，因此一旦找到就跨轮询周期缓存（cachedWindow），
             每轮只需用 IsUsablePlayerWindow 做一次轻量校验，失效了再重新查找。
            
             而窗口内部的进度文本元素则不做跨轮询缓存：其可见性会随用户是否 hover 在进度条上
             而变化（不 hover 时网易云不渲染该文本），查找它的开销仅限于在单个窗口的子树内查找
             Text 控件，相对于整棵桌面树的查找很轻量；同时进度是否"当前可见"这件事本身就需要
             每一轮都重新判断，因此这里选择用轻微的重复查找换取结果的实时准确性。
            
             曲目切换处理：
             generation 变化时说明切歌了，此时重置宽限期计时，等待 UIA 元素重建完成后再恢复抓取；
             期间窗口缓存仍然保留（顶层窗口一般不会因为切歌而销毁重建）。
             </summary>
        </member>
        <member name="M:NeteaseMusicHelper.PublishProgress(System.Int64,System.Int32,System.Int32)">
            <summary>
            将本轮抓取结果写入共享状态，供主线程读取。
            写入前再校验一次 generation：如果在本轮抓取耗时期间曲目已经又发生了切换，
            这份结果就已经过期，直接丢弃，避免污染新曲目的进度缓存。
            </summary>
        </member>
        <member name="M:NeteaseMusicHelper.IsUsablePlayerWindow(Interop.UIAutomationClient.IUIAutomationElement)">
            <summary>
            判断已缓存的窗口元素是否仍然有效可用（标题含 " - " 且不含 "MediaPlayer"）。
            避免每次轮询都重新从桌面根节点查找窗口。
            </summary>
        </member>
        <member name="M:NeteaseMusicHelper.FindNeteasePlayerWindow(Interop.UIAutomationClient.IUIAutomation)">
            <summary>
            在所有 cloudmusic 进程中查找标题包含 " - " 的主播放窗口，返回其 UIA 根节点。
            仅在窗口缓存失效时调用，从桌面 UIA 根节点枚举子窗口进行匹配。
            </summary>
        </member>
        <member name="M:NeteaseMusicHelper.FindProgressElement(Interop.UIAutomationClient.IUIAutomation,Interop.UIAutomationClient.IUIAutomationElement,System.Int32@,System.Int32@)">
            <summary>
            在指定窗口的 UIA 子树内查找所有 Text 控件，逐个尝试解析成
            "MM:SS / MM:SS" 或 "MM:SS | MM:SS" 形式的进度文本，命中即返回。
            查找范围限定在单个窗口子树内，开销较小，因此每轮都重新执行，
            不做跨轮询缓存，以保证能及时反映"进度文本当前是否可见"这一状态。
            </summary>
        </member>
        <member name="M:NeteaseMusicHelper.TryParseProgressText(System.String,System.Int32@,System.Int32@)">
            <summary>
            解析形如 "MM:SS / MM:SS"、"MM:SS | MM:SS" 的进度字符串。
            要求：分隔符两侧都能解析为合法的 MM:SS，且 current &lt;= total。
            </summary>
        </member>
        <member name="M:NeteaseMusicHelper.CreateAutomation">
            <summary>
            依次尝试多种方式创建 UI Automation COM 对象：
            CUIAutomation8（Win8+）→ CUIAutomation（Win7）→ 按 CLSID 激活（兼容性回退）。
            </summary>
        </member>
        <member name="M:NeteaseMusicService.ApplyHoldover(System.String)">
            <summary>
            对候选输出做"短暂失联抑制"：
            - 有效输出（非 None）→ 直接返回，同时刷新缓存
            - 无效输出（None）→ 若缓存未过期则返回缓存，否则清空缓存并返回 None
            
            原因：
            拖动进度条时，网易云可能短暂断开音频会话 / UIA 读取瞬时失败，这会让本服务返回 "None"
            解决思路：若某次轮询的结果为 "None"，但最近 1.5s 内曾经输出过有效结果，
            就继续沿用那份旧输出，等真正闭屏再返回 None，避免把瞬时失联放大成可见事件。
            </summary>
        </member>
        <member name="M:NeteaseMusicService.UpdateProgressChangeTracker(System.Int32)">
            <summary>
            记录进度变化时间戳（在音量 > 0 的情况下也要更新，保证从"有声播放"切到"静音播放"时不误判为暂停）
            </summary>
        </member>
        <member name="M:NeteaseMusicService.IsProgressChangingRecently(System.Int32)">
            <summary>
            在音量为 0 的情况下用于兜底：若进度刚刚发生变化（1.5 秒内），仍认为在播放
            </summary>
        </member>
        <member name="M:WeSingService.FindWeSingWindow">
            <summary>
            查找全民K歌播放窗口的 IUIAutomationElement（标题为 "全民K歌 - {歌曲名}" 的窗口）
            </summary>
        </member>
        <member name="M:WeSingService.ParseProgress(Interop.UIAutomationClient.IUIAutomationElement,System.Int32@,System.Int32@)">
            <summary>
            从 UI Automation 树中解析进度文本 (格式: "00:08 | 04:16")
            </summary>
        </member>
        <member name="M:WeSingService.TryParseProgressText(System.String,System.Int32@,System.Int32@)">
            <summary>
            解析进度文本，支持 "MM:SS | MM:SS" 和 "MM:SS|MM:SS" 格式
            </summary>
        </member>
        <member name="M:WeSingService.TryParseTimeString(System.String,System.Int32@)">
            <summary>
            解析 "MM:SS" 格式的时间字符串为秒数
            </summary>
        </member>
        <member name="M:WeSingService.DetermineStatus(System.Int32)">
            <summary>
            通过进度变化判断播放状态
            如果进度在变化 → Playing；如果进度停滞 → Paused
            </summary>
        </member>
        <member name="M:WindowDetector.GetWindowTitleByHandle(System.IntPtr)">
            <summary>
            通过窗口句柄获取窗口标题（使用 Unicode，避免编码问题）
            </summary>
        </member>
        <member name="M:WindowDetector.GetWindowTitle(System.String)">
            <summary>
            获取指定进程的主窗口标题（Unicode）
            </summary>
        </member>
        <member name="M:WindowDetector.GetWindowTitles(System.String)">
            <summary>
            获取指定进程的所有窗口标题（Unicode）
            </summary>
        </member>
    </members>
</doc>
